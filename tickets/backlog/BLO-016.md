---
id: BLO-016
title: "Disassembly algorithms and MCP tools"
status: backlog
priority: P0
type: feature
created: 2026-03-28
updated: 2026-03-28
dependencies: [BLO-015, BLO-013]
assignee: ai
estimated_complexity: medium
---

# BLO-016: Disassembly algorithms and MCP tools

## Objective

Implement the three high-level disassembly algorithms (`disassemble_range`, `disassemble_function`, `disassemble_at_pc`) as pure domain functions, then expose them through three P0 MCP tools. After this ticket, an LLM can disassemble any range of code, trace a function from entry to return, and view code around the current program counter.

## Context

Built on top of BLO-015 (opcode table + instruction decoder). This ticket adds the algorithms and the thin MCP tool wrappers that connect them to the emulator session. The KB label enrichment (decorating output with knowledge base labels) is intentionally deferred to BLO-017, which depends on BLO-005.

Covers spec §1: `disassemble_range`, `disassemble_function`, `disassemble_at_pc` (all P0).

## Acceptance Criteria

- [ ] `disassemble_range(memory_reader, address, length=None, end_address=None, bank=None) -> list[Instruction]` — decodes instructions from `address` until `length` bytes consumed, `end_address` reached, or 256-instruction cap. At least one of `length` or `end_address` is required.
- [ ] `disassemble_function(memory_reader, entry_point, bank=None) -> dict` — traces forward from `entry_point`, stopping at unconditional `RET` (0xC9), `RETI` (0xD9), or unconditional `JP` (0xC3) that jumps outside the traced range; safety cap of 1024 instructions. Returns `{"instructions": [...], "size_bytes": N}`.
- [ ] `disassemble_at_pc(memory_reader, pc, before=5, after=20) -> list[Instruction]` — backward-scan heuristic to find `before` instructions before `pc`: start from `pc - before * 3`, repeatedly try sequences that align with `pc`; fall back to `pc - before` if alignment fails. Marks the PC instruction with `"current": true`.
- [ ] Three MCP tools (`disassemble_range`, `disassemble_function`, `disassemble_at_pc`) registered in `server.py`, follow the 5-step tool pattern, return structured dicts with instruction lists.
- [ ] Integration tests cover: range decoding with explicit length and with end_address, function tracing stopping at correct terminal opcodes, at_pc backward scan alignment, tool error paths (no ROM loaded, invalid address, address out of range).

## Technical Approach

1. **`src/blobert_mcp/domain/disasm/disassembler.py`** — three pure functions:
   - All take `memory_reader: Callable[[int, int], bytes]` (start_address, length → bytes) to abstract ROM/RAM access
   - No PyBoy, no MCP, no project imports beyond `domain/disasm/decoder.py`
   - `disassemble_function` endpoint detection: `RET` (0xC9), `RETI` (0xD9), `JP nn` (0xC3) — do NOT stop at conditional returns/jumps (`RET cc`, `JR cc`, `JP cc`)

2. **`src/blobert_mcp/tools/disasm.py`** — thin tool wrappers:
   - `disassemble_at_pc` reads current PC from `session.pyboy` registers, then delegates to domain
   - No KB label wiring yet — `label_resolver=None` placeholder deferred to BLO-017
   - Instruction dicts in output: `{"address": "0x1234", "bytes": "CD 34 12", "mnemonic": "CALL", "operands": ["0x1234"], "size": 3}`

3. **Update `src/blobert_mcp/server.py`** — register the 3 new tools from `tools/disasm.py`

4. **TDD sequence**:
   - Write `tests/domain/disasm/test_disassembler.py` first with synthetic byte sequences
   - Write `tests/tools/test_disasm.py` using a mock/fake memory reader
   - Implement until all tests pass

## Dependencies

- BLO-015: SM83 opcode table and instruction decoder (decoder foundation)
- BLO-013: Session & static reading tools (EmulatorSession for reading registers in `disassemble_at_pc`)

## Files Modified

<!-- Filled in during implementation -->

## Notes

- The `memory_reader` callable pattern keeps the disassembler fully decoupled from PyBoy and testable with synthetic bytes.
- `disassemble_at_pc` backward scan: variable-length ISA means scanning backward is ambiguous. The heuristic: try starting from `pc - before*3` bytes back, find the sequence that lands on `pc` with no remainder. If multiple starting points align, prefer the one closest to `pc`.
- Performance: disassembler is called frequently. Keep allocations minimal — avoid building large intermediate structures.
- No KB label enrichment in this ticket — the `label_resolver` parameter exists as `None` default but is never wired. BLO-017 adds the wiring after BLO-005 provides the KB.

## Log

- 2026-03-28: Ticket created. Split from BLO-003 to separate algorithms + tools from the decoder foundation (BLO-015) and from KB integration (BLO-017).
